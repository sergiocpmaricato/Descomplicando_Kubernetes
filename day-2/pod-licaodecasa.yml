apiVersion: v1
kind: Pod
metadata:
  labels:
    run: licaldecasa
  name: licaldecasa
spec:
  containers:
    - image: nginx
      name: webserver
      volumeMounts:
        - mountPath: /giropops
          name: primeiro-emptydir
      resources:
        requests:
          cpu: "0.5"
          memory: "64Mi"
        limits:
          cpu: "1"
          memory: "128Mi"
    - image: alpine
      name: pod-sleeped
      volumeMounts:
        - mountPath: /teste
          name: segundo-emptyDir
      resources:
        requests:
          cpu: "0.3"
          memory: "64Mi"
        limits:
          cpu: "0.5"
          memory: "128Mi"
      
  dnsPolicy: ClusterFirst
  restartPolicy: Always
  volumes:
    - name: primeiro-emptydir
      emptyDir:
        sizeLimit: 256Mi
    - name: segundo-emptyDir
      emptyDir:
        sizeLimit: 128Mi
